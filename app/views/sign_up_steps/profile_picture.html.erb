<%= bootstrap_form_for @user, url:wizard_path, html: {multipart: true} do |f| %>

<h2> Avatar </h2>

<%= f.file_field :avatar, as: :file %>

<div class="actions">
  <%= f.submit "Continue" %>
</div>

<% end %>
