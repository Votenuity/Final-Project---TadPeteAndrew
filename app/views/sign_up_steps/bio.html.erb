<%= bootstrap_form_for @user, url:wizard_path do |f| %>

  <h2> Bio </h2>

  <div class="field">
    <%= f.text_area :bio %>
  </div>

  <div class="actions">
    <%= f.submit "Continue" %>
  </div>

<% end %>
