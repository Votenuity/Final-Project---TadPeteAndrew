<% if user_signed_in? %>
  <% if user.followed_by?(current_user) %>
    <li><%= link_to "Unfollow", unfollow_path(id: user.id), method: :post, class: "btn-primary btn-lg"%></li>
  <% end %>
<% end %>
