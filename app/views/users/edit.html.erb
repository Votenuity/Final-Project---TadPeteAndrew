<%= form_for(@user) do |f| %>

  <div class="field">
    <%= f.label :bio%><br>
    <%= f.text_area :bio%>
  </div>

  <div class="actions">
    <%= f.submit "Submit Changes to Bio" %>
  </div>

<% end %>
